#!/usr/bin/env bash

cd ..

chmod +x $PWD/commands/launchServer.command
open -a Terminal $PWD/commands/launchServer.command

chmod +x $PWD/commands/launchShops.command
open -a Terminal $PWD/commands/launchShops.command

chmod +x $PWD/commands/launchCustomers.command
open -a Terminal $PWD/commands/launchCustomers.command
